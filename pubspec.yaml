name: alarm  # プロジェクトの名前をここに書きます
description: A new Flutter project

publish_to: 'none' # Remove this line if you're publishing your package

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0  # ここに最新バージョンを追加します

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
